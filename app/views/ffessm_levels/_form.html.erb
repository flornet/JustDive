<%= form_for(@ffessm_level) do |f| %>
  <% if @ffessm_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ffessm_level.errors.count, "error") %> prohibited this ffessm_level from being saved:</h2>

      <ul>
      <% @ffessm_level.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
