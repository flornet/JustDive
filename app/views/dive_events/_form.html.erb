<%= form_for(@dive_event) do |f| %>
  <% if @dive_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dive_event.errors.count, "error") %> prohibited this dive_event from being saved:</h2>

      <ul>
      <% @dive_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dive_club_id %><br />
    <%= f.number_field :dive_club_id %>
  </div>
  <div class="field">
    <%= f.label :start_date %><br />
    <%= f.date_select :start_date %>
  </div>
  <div class="field">
    <%= f.label :end_date %><br />
    <%= f.date_select :end_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
