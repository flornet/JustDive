<%= form_for(@admin_dive_club, :html => {:class => 'well'}) do |f| %>
  <% if @admin_dive_club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_dive_club.errors.count, "error") %> prohibited this admin_dive_club from being saved:</h2>

      <ul>
      <% @admin_dive_club.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :ffessm_licence_number %>
    <%= f.text_field :ffessm_licence_number %>
  </div>
  <div class="actions">
    <%= f.submit "Save", :class => 'btn btn-primary' %>
  </div>
<% end %>
